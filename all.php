CREATE TABLE forgot (
    c_first_name varchar(255) NOT NULL,
    c_last_name varchar(255) NOT NULL,
    c_password1 varchar(255) NOT NULL,
    c_id varchar(255) NOT NULL,
    c_phone varchar(255) NOT NULL,
    c_mail varchar(255),
    PRIMARY KEY (c_id)
);
create db for reload password